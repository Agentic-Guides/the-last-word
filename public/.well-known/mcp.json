{
  "name": "the-last-word",
  "description": "The Last Word — human approval gate for irreversible AI agent actions. Exposes the notary.approve and notary.delete tools for human-in-the-loop approval.",
  "tools": [
    {
      "name": "notary.approve",
      "description": "Approve an irreversible payment action. Requires human confirmation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "amount": { "type": "string", "description": "The amount to pay, e.g. 1200.00" },
          "payee": { "type": "string", "description": "The payee name" }
        },
        "required": ["amount", "payee"]
      }
    },
    {
      "name": "notary.delete",
      "description": "Delete data irreversibly. Requires human confirmation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "target": { "type": "string", "description": "The data/account to delete, e.g. my account and all data" }
        },
        "required": ["target"]
      }
    }
  ]
}
