# The Last Word

> Human approval gate for irreversible AI agent actions. A WebMCP human-in-the-loop trust layer.

The Last Word is a WebMCP Declarative API app that lets a human approve — and cryptographically sign — irreversible actions that an AI agent wants to take (payments, transfers, deletions, contract submissions). It is the "human stays in control" layer for agentic commerce.

## When to use this site

Use The Last Word when an AI agent is about to perform an **irreversible action** on behalf of a person or business and a human must give the final approval:

- Approving a payment or money transfer prepared by an agent
- Signing off on a contract, invoice, or irreversible submission
- Auditing who approved what, when, with a session-scoped ECDSA signature and SHA-256 fingerprint
- Demonstrating a human-in-the-loop approval gate for WebMCP / agentic apps

Do NOT use this site for reversible actions, read-only queries, or anything that does not need human sign-off.

## How an agent uses it

The site exposes two WebMCP Declarative API tools:

### `notary.approve`

Approve an irreversible payment action. Requires human confirmation.

**Parameters:**
- `amount` (string): the amount to pay, e.g. `1200.00`
- `payee` (string): the payee name

### `notary.delete`

Approve an irreversible data deletion. Requires human confirmation.

**Parameters:**
- `target` (string): what to delete, e.g. `my account and all data`

**Flow (both tools):**
1. Agent fills the form with the required parameters and submits.
2. The tool call is **held open** via `respondWith()` — the agent's call does not resolve until the human decides.
3. The human reviews the prepared action (amount/payee, or delete target, plus irreversibility warning).
4. The human clicks **Approve & Sign** — a real ECDSA digital signature (P-256) is generated over the approval payload — or **Reject** to block it.
5. The held tool call resolves with the human's decision, and the audit log records who approved what, when, with the signature and SHA-256 fingerprint.

## Key URLs

- Home / live app: https://the-last-word.pages.dev/
- Source (open source, MIT): https://github.com/Agentic-Guides/the-last-word
- Sitemap: https://the-last-word.pages.dev/sitemap.xml
- About: https://the-last-word.pages.dev/about
- Contact: https://the-last-word.pages.dev/contact
- Privacy: https://the-last-word.pages.dev/privacy

## Contact

- Project: The Last Word (WebMCP Challenge)
- Maintainer: Agentic-Guides
- Email: aiaiaiaiaiai0x@gmail.com
